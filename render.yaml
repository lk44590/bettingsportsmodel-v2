services:
  - type: worker
    name: betting-card-worker
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python generate_daily_card.py --skip-provider-sync
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
